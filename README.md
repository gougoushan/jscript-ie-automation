# jscript-ie-automation
Important: NavigateComplete2 event will not trigger when calling navigate in NavigateComplete2 callback function, instead of using ie.document.parentWindow.eval("location.href=''")
